# plan module

Plan IR: types, load/save, normalization. No execution or AI logic here.

Files:
- `types.rs` — Plan/StepConfig types
- `normalization.rs` — normalizes/cleans plans
- `bundle.rs` — plan bundling

You own this dir only. Read other `src/` dirs for context, never edit them.
